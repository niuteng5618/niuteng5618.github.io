---
layout: post
title: "mysql"
display_title: "mysql"
display_filename: "mysql.md"
date: 2026-05-30
primary_category: "计算机基础"
secondary_category: "数据库"
series: "MySQL"
primary_category_order: 2
secondary_category_order: 2
series_order: 1
post_order: 71
categories:
  - 计算机基础
  - 数据库
  - MySQL
tags:
  - MySQL
  - SQL
  - 索引
  - 事务
toc: true
comments: false
author: niuteng5618
---
删不掉表时，先关闭外键约束。

SET foreign_key_checks = 0;  // 先设置外键约束检查关闭

drop table Student restrict;  // 删除数据，表或者视图

SET foreign_key_checks = 1; // 开启外键约束检查，以保持表结构完整性

———————————————————————————————————

<font style="color:#F33232;">CASE 表达式进行替换 （注意逻辑顺序，不能颠倒）</font>

select * ,

<font style="color:#F33232;">case</font>

when GRADE>=30 then '优秀'

when GRADE>=20 then '良好'

when GRADE>=10 then '及格'

else '不及格'

<font style="color:#F33232;">end as</font> 成绩等级

from stu

<font style="color:#F33232;">0，1表示男女</font>

select *,

CASE

when 性别=1   then '女'

when 性别=0  then '男'

end  as Sex

from xsb

<font style="color:#f33232;">查询 XSB 表中第 11-20 条记录</font>，输出所有列  select top 10 * from xsb where 学号 not in (select top 10 学号 from xsb)

<font style="color:#F33232;">创建</font>

选择创建1：Ssex char(2) check(Ssex in('男','女'))default '男'

选择创建2：Sage int check(Sage >=16 and Sage<=40) default 21

<font style="color:#F33232;">添加列</font>

alter table tableName add columnName varchar(30)  

<font style="color:#F33232;">断言</font>

<font style="color:#000000;">create ASSERTION ASS1  //ASS1为断言名</font>

<font style="color:#000000;">check(______)    //条件语句</font>

<font style="color:#000000;">每次执行语句都检查一次断言，当断言为假则拒绝执行。</font>

<font style="color:#FF0001;">触发器(</font><font style="color:#D041E1;">看不懂)</font>

<font style="color:#F33232;">删除数据</font> delete <u>from</u> Stu where Sname='LIhua'

<font style="color:#F33232;">根据已有表创建新表</font>

create table sc_copy (select * from sc)



**<font style="color:#F33232;">改</font>**

改列名 ALTER TABLE Sstudent CHANGE sss Sas char(20)

改列类型 alter table student modify  column sss  char(20)

改表名  alter table student rename abc



<font style="color:#F33232;">查询</font>

<font style="color:#f33232;"> a</font><font style="color:#F33232;">s 用法</font>

统计各专业的学生人数： select 专业,count(*) 

as 学生人数 

from xsb group by 专业

group by <font style="color:#F33232;">聚集函数</font> 值相同的分成一个组  不能用where 要用having

order by 排序  asc/desc 顺序

涉及空值 用 where Grade IS NULL

正则表达式：字符串用 LIKE  、NOT LIKE  ,%匹配任意个字符、_匹配一个字符。当要查询的字符串中有‘_’时：where Cname LIKE 'abc\_de' escap '\'

正则表达式       [华涛]，表示'华'或者'涛'出现一次

DISTINCT 去重 写在前面

COUNT 统计个数



自然连接

select student.Sno,Sname,Ssex,Sage,Sdept,Cno,Grade

from student,sc

where student.Sno=sc.Sno    //注意 这里用的是where

外连接，<u><font style="color:#F33232;">左</font></u>外连接。

select student.Sno,Sname,Ssex,Sage,Sdept,Cno,Grade

from student

<u>left outer join</u> sc

on(student.Sno=sc.Sno)       //注意 这里要用 on

多表连接 (不太会)

select student.Sno,Sname,Cname,Grade

from student,sc,course

where student.Sno=sc.Sno<font style="color:#F33232;"> and</font> sc.Cno=course.Cno

exists 代表‘存在’，带有exists为此的子查询不返回任何数据，只返回ture or false

修改表格数据 ---修改101号年龄为22

update Student SET Sage=22  where Sno='101'

集合查询：参加集合操作的各查询结果的列数必须相同，对应的数据类型也必须相同

**<font style="color:#F33232;">union 并集</font>**

**<font style="color:#F33232;">intersect 交集</font>**

**<font style="color:#F33232;">except 差集</font>**

子查询放在 from后面叫派生表，as关键字可以省略，但必须为派生表指定一个别名(没看懂)

插入 insert into；将查询结果插入表中,

insert into Dept_age(Sdept,Avg_age)

select Sdept,AVG(Sage)

From student

group by Sdept

属性定义时 加了UNIQUE 就不能取 空值 了

<font style="color:#F33232;">视图</font>

创建视图：

create view IS_Student(a,b,c) 

as select Sno,Sname,Sage

from Student where Sdept='IS'

(WITH CHECK OPTION)         //这句话 表示 以后在对该视图进行插入、修改、删除时，关系数据库管理系统会自动加上 Sdept='IS' 的条件

删除视图：如果S1视图还导出S2视图了，那么S1无法直接删除，需要加 cascade





<font style="color:#F33232;">权限</font>

<font style="color:#F33232;">赋予</font>user1 student表 的 <u>select权限</u>：

grant select 

on table student

to user1

赋予 user1 student表 的 <u>所有权限</u>

grant all privileges 

on table student

to user1

赋予 <u>所有用户</u> student表 的 <u>所有权限</u>

grant <u>all privileges </u>

on table student

to <u>public</u>

查询student表和<u>修改学生学号</u>的权限赋予user1

grant <u>update(Sno)</u>,select

on table student

to user1

[with grant option]       //这句话的作用是:  允许user1用户将此权限赋予其他用户

<font style="color:#F33232;">权限收回?</font>?

收回 user1 对student表的修改Sno权限

revoke update(Sno) 

on table student

from user1 <u>[CASCADE]</u>        //CASCADE 作用是收回级联权限，即同时收回由

                                                 user1赋予别人的权限

<font style="color:#F33232;">创建新用户</font>：create user <niuteng> [WITH] [DBA|RESOURCE|CONNECT]

//创建的新用户只有 D R C三种权限

___________________________________________________________________

创建用户创建模式创建基本表和视图          登录数据库

DBA         √          √                         √                                       √

RESOURCE                                                     √                                       √

CONNECT                                                                                                 √

___________________________________________________________________

<font style="color:#F33232;">创建角色</font>：CREATE ROLE   R1      //角色作用:     简化授权过程

赋予角色 R1 权限：

GRANT SELECT,UPDATE,INSERT

ON TABLE Student

TO R1

将角色R1赋予其他人

GRANT R1

TO 王平，张萌

<font style="color:#F33232;">存储过程、触发器</font>

<font style="color:#000000;">存储过程优点：模块化程序设计、提高执行速度、降低网络通信量、提高系统安全性</font>

<font style="color:#F33232;">存储结构</font>：类似函数

![](/images/yuque/071-mysql/image-1-3578cf31.png)



![](/images/yuque/071-mysql/image-2-fd421efd.png)











触发器：在对A表操作时，B表中对应的A表中的数据也要被操作。

![](/images/yuque/071-mysql/image-3-3b1e857a.png)

<font style="color:#F33232;">触发器</font>类型：INSERT触发器、DELETED触发器、UPDATE触发器

还可以分为DML（数据操控）触发器和DDL(数据定义)触发器

![](/images/yuque/071-mysql/image-4-41daab18.png)

触发器有2个临时表：inserted表和deleted表

![](/images/yuque/071-mysql/image-5-7fbba97c.png)



![](/images/yuque/071-mysql/image-6-aef88e76.png)



![](/images/yuque/071-mysql/image-7-3d6c181e.png)
